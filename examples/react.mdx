# React Example

```Layerfile
#This is an example webapp.io configuration for React!
FROM vm/ubuntu:18.04

#Allocate memory
MEMORY 2G

# To note: Layerfiles create entire VMs, *not* containers!
# Install tools needed to run the react app
RUN curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -
RUN sudo apt-get install -y nodejs
RUN sudo npm install npm@latest -g

# node is a memory hog
ENV NODE_OPTIONS=--max-old-space-size=8192

# Copy files from repository into the virtual machine
COPY . .

# Install dependencies for React project
RUN npm install

# Start the React app
RUN BACKGROUND npm start

# Create a unique link to share the app in this runner.
# Every time someone clicks the link, we'll wake up this staging server.
EXPOSE WEBSITE http://localhost:3000
```

[Try this example live](/sign-up)

##React Detailed Walkthrough

Need a detailed walkthrough to set up a production-ready preview environment for a React application?
[Click here.](/blog/how-to-set-up-production-ready-preview-environments-for-your-react-application/)
